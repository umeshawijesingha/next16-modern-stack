'use cache'       //To use use cache in a function function should be an async function. It only works with server components, not client components
export default async function Analytics() {
  return (
    <div>Analytics</div>
  )
}

